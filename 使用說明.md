# 使用說明

## 這個網站怎麼運作

把它想成一本放在 GitHub 的活頁夾：每一頁是一個 `.md` 檔，網站只負責翻頁、找字、記錄哪張卡看過、哪一章讀完。

## 四種紙

三個分區是三種不同的紙，寫法和長相都不同；第四種「考場」不是筆記，是考卷：

| 分區 | 是哪種紙 | 怎麼用 |
|---|---|---|
| **學科** | 卡片紙 | `##` 一張卡，有「標記已複習」「自測遮答案」；地圖頁可以打勾 |
| **學習** | 白紙 | 一個主題一頁，整頁自由寫，沒有按鈕。想放截圖、表格、清單都行，右下角「編輯這頁」就是全部 |
| **隨寫** | 日記紙 | 按「今天寫一筆」自動帶日期、存進當月檔，最新在最上面；選心情、打 `#標籤`，之後點標籤可以篩 |
| **考場** | 考卷 | 歷年考古題都在裡面。選一份、計時、作答、交卷、批改；錯的一鍵變錯題卡。作文是稿紙、選擇題是答案卡、填充和問答是答案卷 |

想改哪個分區用哪種紙，在 `index.html` 最上面 `SECTION_MODE` 那一行改。新增的分區預設是白紙。

## 小筆記：在卡片上直接寫

每張卡下面有「✎ 加筆記」。按下去出現一格便利貼，查到的、想到的、要背的直接打，Ctrl + Enter 或按「存到這張卡」，會存成這張卡最後面的一段「**我的筆記**（日期）」，黃底顯示。同一張卡可以加很多次，每次一段、各帶日期。想改整張才需要「改整張」。

筆記裡打 `[[概念名]]`（照考點頁的名字）會變成連結，點了跳到同一科概念頁的那張卡。錯題卡連概念卡、概念卡連考點，就是這樣接起來的。

## 訪客模式：別人看到的是空白的

網站是公開的，但複習紀錄、地圖打勾、考試成績只有貼了鑰匙的瀏覽器才會去讀 GitHub 上的紀錄檔；沒貼鑰匙（別人、無痕視窗、新裝置）看到的是一片空白，做的紀錄也只留在那台瀏覽器。注意這只擋「紀錄」，卡片、筆記、作文這些內容本來就是公開的，不想給人看的東西不要寫進去。

## 手機：加到桌面

iPhone：用 Safari 開網站 → 下方「分享」→「加入主畫面」→ 右上角「新增」。桌面會出現橘色 D 圖示，點開是全螢幕、沒有網址列，跟 App 一樣。Android：Chrome 右上角「⋮」→「加到主畫面」。

要知道的兩件事：

- 加到桌面的那個「App」和 Safari 是兩個分開的瀏覽器，鑰匙、複習紀錄都不共用。第一次在 App 裡開，到「設定」再貼一次鑰匙，之後紀錄就會從 GitHub 同步過來。
- 網站每次打開都從 GitHub 讀最新內容，所以要有網路。捷運上訊號斷斷續續時，考場的作答草稿存在手機裡不會掉，交卷時再連上就好。

手機上適合做的：組合卷 10 題、看「今天先看這些」、翻概念卡。作文和申論還是用電腦或紙筆。

## 圖片

在編輯器裡 **Ctrl+V 直接貼圖**、把圖拖進來，或按「🖼 插入圖片」選檔。網站會把圖壓到最長邊 1600px 存進 repo 的 `assets/年/月/`，並在游標處放上 `![](assets/…)`。點文章裡的圖會放大。小張 PNG（400KB 以下）不壓，保留清晰。

## 學科怎麼讀

每一科三頁，是三個層次：

| 頁 | 是什麼 | 怎麼用 |
|---|---|---|
| **地圖** | 這科的骨架。`##` 是母概念（章），底下方格是子概念 | 讀懂一項就打勾。看到考古題出現某個名詞，先回地圖找它的位置，把同一章的兄弟姊妹一起看完 |
| **錯題** | 做錯的題目，一題一張卡 | 自測模式先遮答案，想完再點開；看完按「標記已複習」 |
| **考點** | 六年考古題依考點分類的索引，一個考點一張 | 看哪些考過很多次、每題怎麼問。按卡片裡的「＋ 開這張概念卡」會在概念頁開一張同名的卡；寫過的變成「看我寫的 →」。這頁不進複習，也不會被隨機抽到 |
| **概念** | 一個理論一張卡，內容自由寫 | 標題照「考點」頁的名字（例如【行】民營化 BOT/BOO 模式），內容想怎麼寫就怎麼寫：抄書、自己的話、比喻、口訣都行。「概念卡（有欄位）」範本只是建議 |

地圖上的 〔A〕〔B〕 是兩場考試的代號：A 是 1 月報名那場，B 是 6 月報名那場；沒標的兩場都考。這樣不靠教科書也知道範圍到哪、自己漏了哪一章。

沒買書怎麼補內容：地圖的每一格就是一個搜尋關鍵字。在 Claude 專案裡貼「用白話和比喻解釋：〔那一格的文字〕，再給一題練習」，讀完打勾，答錯的貼進錯題。

| 位置 | 做什麼 |
|---|---|
| 上方 **學科／學習／隨寫** | 三個分區。學科首頁一科一張卡，顯示地圖進度和卡數 |
| 首頁 **今天先看這些** | 超過 7 天沒看的卡，最久沒看的排前面 |
| 首頁 **這週** | 最近 7 天每天複習幾次、總卡數、看過、該再看、地圖讀完百分比 |
| **🎲 隨機抽一張** | 從到期的卡裡抽一張；沒有到期就抽沒看過的 |
| **自測模式** | 進到頁面會先遮住答案，點卡片才顯示 |
| 右上角 **搜尋**（快捷鍵 `/`） | 搜所有卡片的標題和內文，點結果直接跳到那張卡 |
| 右下角主按鈕（快捷鍵 `n`） | 會隨位置變：學科頁是「加一張卡」、學習頁是「編輯這頁」、隨寫是「今天寫一筆」、首頁是「新頁面」 |
| 右下角 **✎ 編輯這頁** | 改整頁 Markdown（白紙頁面併進主按鈕） |
| 卡片下方 **✎ 改這張** | 開編輯器並定位到那張卡 |
| 右上角 **設定** | 貼 GitHub 鑰匙、調「幾天算該再看」。綠點＝已連上 |

## 第一次：貼 GitHub 鑰匙（3 分鐘，只做一次）

網站要能替你寫檔案，需要一把只能開這個 repo 的鑰匙。

1. 開 [GitHub → 產生 Fine-grained token](https://github.com/settings/personal-access-tokens/new)
2. Token name 打 `daybook`，Expiration 選 **1 year**
3. Repository access 選 **Only select repositories**，勾 `dailybook`
4. Permissions → Repository permissions → **Contents** 改成 **Read and write**，其他不用動
5. 按 **Generate token**，把 `github_pat_…` 整串複製
6. 回到 Daybook，右上角「設定」→ 貼上 → 「測試並儲存」，看到綠點就完成

鑰匙只存在這台瀏覽器。換電腦或手機要再貼一次；弄丟了就到 GitHub 刪掉舊的、重產一把。沒貼鑰匙也能看、能搜、能標記複習，只是紀錄留在這部電腦、寫不了筆記。

## 一張卡長什麼樣

每個 `## ` 開頭的標題就是一張卡。錯題卡的固定格式：

```
## 【行政】115-Q01 明茲伯格五種協調機制

**題目**：明茲伯格提出 5 種協調機制，下列何者非屬之？
**選項**：(A) 人員標準化 (B) 產出標準化 (C) 相互調適 (D) 直接監督
**答案**：A
**我選**：C
**為什麼錯**：把「技術標準化」記成「人員標準化」
**觀念一句話**：五種是相互調適、直接監督、工作流程／產出／技術標準化，沒有「人員」
**易混淆**：明茲伯格的「五種組織構型」是另一組東西
```

標題規則：`【科目簡稱】年度-題號 一句話`，之後搜「115」「明茲伯格」都找得到。自測模式會遮住「答案」以後的欄位，「題目」「選項」照常顯示。

## 請 Claude 幫你產卡

在 Claude 專案裡答完題，貼這句：

> 把我剛答錯的題目整理成 Daybook 錯題卡格式（## 【科目】115-Q題號 一句話標題，接題目、選項、答案、我選、為什麼錯、觀念一句話、易混淆），直接給 Markdown。

拿到後：開對應科目 → ＋ 新筆記 → 貼上 → 存到 GitHub。

## 複習節奏

- 平日打開首頁，先清「今天先看這些」，或按「隨機抽一張」。
- 開自測模式，先想答案再點開對答案。
- 看完按「標記已複習」；同一天重按不會重複計次。
- 複習紀錄存在 `data/reviews.json`，地圖打勾存在 `data/progress.json`，換裝置都在。

## 導讀：沒有課本時的第一章

「學習」分區有六頁導讀（行政學、法學、管理學、英文、國文、經濟學）和一張英文單字表。每頁把該科六年考過的考點收成十幾條線，用故事講一遍，每條線最後列考點名字和考過次數。用法：先讀一遍不做筆記 → 做題 → 錯的回導讀找它在哪條線 → 去考點頁按「開這張概念卡」。內容是 Claude 寫的，理論架構可信，數字（年份、條號、天數）背之前查一次。

## 考場

上方分頁最右邊的「考場」。題庫在 `data/bank/`，一份考卷一個 JSON，`index.json` 是清單；成績存在 `data/exams.json`，有貼鑰匙就會同步。

### 三種考卷

| 題型 | 長什麼樣 | 交卷之後 |
|---|---|---|
| **作文**（稿紙） | 題目在上面、稿紙在下面、右上角倒數。邊打邊看字數、段落數、平均每段字數；標點有問題會在下面亮提醒：半形標點、「」沒成對、段尾沒句號、一句太長沒逗號、連續標點 | 鎖定不能改。「複製給 Claude 批改」會把題目、用時、正文和批改指令一起複製，貼到 Claude 專案；「存成作文卡」寫進作文練習頁，批改結果回去補 |
| **選擇題**（答案卡） | 整份出題、倒數計時、右邊答案卡看哪題還沒答。交卷時有空白會擋一下：不倒扣，一定要猜 | 立刻批改：對的綠、錯的紅，顯示正確答案和你選的。「存成錯題卡」把錯的寫進對應科目的錯題頁，「為什麼錯」「觀念一句話」自己回去補；「複製錯題給 Claude 解析」貼去問 |
| **填充／問答／申論**（答案卷） | 填充是一格一格填；問答和申論可以打字，也可以勾「用紙筆寫，這裡只計時」 | 填充會並排官方答案，機器先比對一次，寫法不同但意思對的自己按 ✓；問答複製給 Claude 批改，拿到分數回來填、存紀錄 |

考試時間預設是考場上大概分到的時間（例如 A 考作文 40 分鐘、英文 40 分鐘、行政學 30 分鐘），開始前可以改。時間到會自動交卷。

### 組合卷

考場最上面。勾科目、選「全部／沒做過的／錯過的」、選題數，從歷年題目隨機抽。「錯過的」是最後一次作答還是錯的題目：答對一次就離開這個池子，再錯又回來。

### 寫到一半離開

草稿存在這台瀏覽器，時間照算。回到同一份考卷會自動接續；首頁的考場小格也會提醒「有一份沒交的卷」。換電腦不會帶著走。

### 題庫哪裡來

考古題 PDF 交給 Claude 轉成 JSON。之後新的年度出來，把試題和答案 PDF 貼給 Claude 說「轉成 Daybook 題庫格式」，拿到 `A-116-1.json` 這種檔放進 `data/bank/`，再把它加進 `index.json`。全站不出現考試名稱，考卷只用 A（1 月報名）／B（6 月報名）代號。

## 作文與批改

作文練習頁一篇一張卡。在考場寫完按「存成作文卡」會自動建一張；貼到 Claude 專案批改後，把分數和缺的東西補進「AI 批改」欄。

## 容量會不會爆

不會，先給數字：GitHub 一個 repo 建議在 1GB 以內，單檔上限 100MB，Pages 網站上限 1GB。文字幾乎不占空間，這個 repo 寫十年的筆記大概還不到 5MB。真正吃空間的是圖，壓過的截圖一張 100～200KB，1GB 可以放五千多張。

比較重要的是「會不會越來越亂」，這點靠三個設計：

- 外殼只有一個 `index.html`，它不會因為筆記變多而變大。資料多了，變重的是 `.md` 和 `assets/`，兩邊分開。
- 日記一個月一檔、學科一科三檔、圖片按年月分資料夾，每個檔案都有自然的邊界，不會出現一個一萬行的檔。
- 首頁每次會讀所有頁面算數字。頁面在 100 個以內感覺不到；若某天真的變慢，只要加一層快取，不用重寫。

真的想瘦身：舊年份的 `assets/2026/` 整個資料夾搬去雲端硬碟、筆記裡的圖連結改成外部網址，repo 立刻回到幾 MB。

## 想改分區或名稱

目錄在 `_sidebar.md`（沿用舊檔名）。`- 學科` 這種沒連結的一行是分區，底下縮排的連結是頁面；路徑中間那層資料夾（`學科/管理學/…`）就是科目，首頁會自動一科一張卡。新增分區就多加一行，上方分頁會自動長出來。分區顏色在 `index.html` 的 `.sec-學科`、`.sec-學習`、`.sec-隨寫` 三行。

## 出問題時

- **存檔說 401／403**：鑰匙過期或權限不對，到「設定」重貼。
- **剛存的東西不見**：紅點表示沒連上，網站顯示的是 GitHub Pages 的舊版，等 1 分鐘重新整理。
- **頁面顯示「還沒有內容」**：檔案還沒建，按頁面上的按鈕就會建。
- **剛貼的圖破圖**：GitHub Pages 還沒更新，網站會自動改抓 GitHub 原始檔，等一下重新整理即可。

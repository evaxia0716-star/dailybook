# 使用說明

## 這個網站怎麼運作

把它想成一本放在 GitHub 的活頁夾：每一頁是一個 `.md` 檔，網站只負責翻頁、找字、記錄哪張卡看過、哪一章讀完。

## 學科怎麼讀

每一科三頁，是三個層次：

| 頁 | 是什麼 | 怎麼用 |
|---|---|---|
| **地圖** | 這科的骨架。`##` 是母概念（章），底下方格是子概念 | 讀懂一項就打勾。看到考古題出現某個名詞，先回地圖找它的位置，把同一章的兄弟姊妹一起看完 |
| **錯題** | 做錯的題目，一題一張卡 | 自測模式先遮答案，想完再點開；看完按「標記已複習」 |
| **概念** | 名詞卡：一句話、比喻、怎麼考、跟它很像的 | 申論的名詞解釋直接從這裡背 |

地圖上的 〔A〕〔B〕 是兩場考試的代號：A 是 1 月報名那場，B 是 6 月報名那場；沒標的兩場都考。這樣不靠教科書也知道範圍到哪、自己漏了哪一章。

沒買書怎麼補內容：地圖的每一格就是一個搜尋關鍵字。在 Claude 專案裡貼「用白話和比喻解釋：〔那一格的文字〕，再給一題練習」，讀完打勾，答錯的貼進錯題。

| 位置 | 做什麼 |
|---|---|
| 上方 **學科／學習／隨寫** | 三個分區。學科首頁一科一張卡，顯示地圖進度和卡數 |
| 首頁 **今天先看這些** | 超過 7 天沒看的卡，最久沒看的排前面 |
| 首頁 **這週** | 最近 7 天每天複習幾次、總卡數、看過、該再看、地圖讀完百分比 |
| **🎲 隨機抽一張** | 從到期的卡裡抽一張；沒有到期就抽沒看過的 |
| **自測模式** | 進到頁面會先遮住答案，點卡片才顯示 |
| 右上角 **搜尋**（快捷鍵 `/`） | 搜所有卡片的標題和內文，點結果直接跳到那張卡 |
| 右下角 **＋ 新筆記**（快捷鍵 `n`） | 在目前頁面加一張卡；在首頁或分區則是開新頁面 |
| 右下角 **✎ 編輯這頁** | 改整頁 Markdown |
| 卡片下方 **✎ 改這張** | 開編輯器並定位到那張卡 |
| 右上角 **設定** | 貼 GitHub 鑰匙、調「幾天算該再看」。綠點＝已連上 |

## 第一次：貼 GitHub 鑰匙（3 分鐘，只做一次）

網站要能替你寫檔案，需要一把只能開這個 repo 的鑰匙。

1. 開 [GitHub → 產生 Fine-grained token](https://github.com/settings/personal-access-tokens/new)
2. Token name 打 `daybook`，Expiration 選 **1 year**
3. Repository access 選 **Only select repositories**，勾 `dailybook`
4. Permissions → Repository permissions → **Contents** 改成 **Read and write**，其他不用動
5. 按 **Generate token**，把 `github_pat_…` 整串複製
6. 回到 Daybook，右上角「設定」→ 貼上 → 「測試並儲存」，看到綠點就完成

鑰匙只存在這台瀏覽器。換電腦或手機要再貼一次；弄丟了就到 GitHub 刪掉舊的、重產一把。沒貼鑰匙也能看、能搜、能標記複習，只是紀錄留在這部電腦、寫不了筆記。

## 一張卡長什麼樣

每個 `## ` 開頭的標題就是一張卡。錯題卡的固定格式：

```
## 【行政】115-Q01 明茲伯格五種協調機制

**題目**：明茲伯格提出 5 種協調機制，下列何者非屬之？
**選項**：(A) 人員標準化 (B) 產出標準化 (C) 相互調適 (D) 直接監督
**答案**：A
**我選**：C
**為什麼錯**：把「技術標準化」記成「人員標準化」
**觀念一句話**：五種是相互調適、直接監督、工作流程／產出／技術標準化，沒有「人員」
**易混淆**：明茲伯格的「五種組織構型」是另一組東西
```

標題規則：`【科目簡稱】年度-題號 一句話`，之後搜「115」「明茲伯格」都找得到。自測模式會遮住「答案」以後的欄位，「題目」「選項」照常顯示。

## 請 Claude 幫你產卡

在 Claude 專案裡答完題，貼這句：

> 把我剛答錯的題目整理成 Daybook 錯題卡格式（## 【科目】115-Q題號 一句話標題，接題目、選項、答案、我選、為什麼錯、觀念一句話、易混淆），直接給 Markdown。

拿到後：開對應科目 → ＋ 新筆記 → 貼上 → 存到 GitHub。

## 複習節奏

- 平日打開首頁，先清「今天先看這些」，或按「隨機抽一張」。
- 開自測模式，先想答案再點開對答案。
- 看完按「標記已複習」；同一天重按不會重複計次。
- 複習紀錄存在 `data/reviews.json`，地圖打勾存在 `data/progress.json`，換裝置都在。

## 作文與批改

作文練習頁一篇一張卡。寫完貼到 Claude 專案請它用 25 分制批改，把分數和缺的關鍵字補進卡片的「AI 批改」欄。

## 想改分區或名稱

目錄在 `_sidebar.md`（沿用舊檔名）。`- 學科` 這種沒連結的一行是分區，底下縮排的連結是頁面；路徑中間那層資料夾（`學科/管理學/…`）就是科目，首頁會自動一科一張卡。新增分區就多加一行，上方分頁會自動長出來。分區顏色在 `index.html` 的 `.sec-學科`、`.sec-學習`、`.sec-隨寫` 三行。

## 出問題時

- **存檔說 401／403**：鑰匙過期或權限不對，到「設定」重貼。
- **剛存的東西不見**：紅點表示沒連上，網站顯示的是 GitHub Pages 的舊版，等 1 分鐘重新整理。
- **頁面顯示「還沒有內容」**：檔案還沒建，按「在這頁加第一張卡」就會建。

# n8n

自動化工具的學習筆記。一個概念一張卡，看完一樣按「標記已複習」。

## Workflow、Node、Trigger 是什麼

**一句話**：Workflow 是一條流程，Node 是流程上的一個步驟，Trigger 是讓流程開始跑的那個開關。

**比喻**：Workflow 是食譜，Node 是每一個步驟，Trigger 是「客人點餐了」。

**怎麼用**：先想「什麼事發生時要啟動」（Trigger），再想「之後要做哪幾步」（Nodes）。
